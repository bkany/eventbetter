<p id="notice"><%= notice %></p>

 <div class="jumbotron">
        <div class="container">
          <h1 class="display-3">Event brite, event brighter, even better !</h1>
          <p>Avec Event Better, réservez n'importe quel événement et participez à des conférences en ligne, depuis votre canapé !</p>
          <p><a class="btn btn-primary btn-lg" href="/" role="button">Voir tous les événemets</a></p>
        </div>
</div>

<h1>Events</h1>




<div class="card-columns">
	<% @events.each do |event| %>
  <div class="card">
  
    <img class="card-img-top" src="https://picsum.photos/215/160?random=1" alt="Card image cap">
    <div class="card-body">
      <h5 class="card-title">Titre de l'évènement n°<%= event.id %> : <%= event.title %></h5>
      <p class="card-text"><%= event.description %></p>
      <p><%= link_to 'Show', event %> 
      		
      		<% if user_signed_in? %>
      		- <%= link_to 'Edit', edit_event_path(event) %> - <%= link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' } %></p> <% end %>
      		
    </div>
  </div>

	<% end %>
</div>






<br>

<%= link_to 'New Event', new_event_path %>
